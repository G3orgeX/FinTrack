export class Categoria {
     constructor(id, nombre, descripcion, factor) {
        this.id = id;
        this.nombre = nombre;
        this.descripcion = descripcion;
        this.factor = factor;
    }
    
}